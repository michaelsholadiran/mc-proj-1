export type Department = {
  departmentName: string;
  departmentCode: string;
  departmentId: string;
};

export type CreateDepartmentPayload = {
  departmentName: string;
  departmentCode: string;
};
